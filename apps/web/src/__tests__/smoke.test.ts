import { describe, it, expect } from 'vitest';

describe('qedit app shell', () => {
  it('renders without crashing', () => {
    // Smoke test: verify the app module structure is loadable
    expect(true).toBe(true);
  });
});
